#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints all possible different combination of three digits.
 * Return: Always 0 (success)
 */
int main(void)
{
	int ones = '0';
	jnt tens = '0';
	int hundreds = '0';

	for (hundreds = '0'; hundred <= '9'; hundred++)
	{
		for (tens = '0'; tens <= '9' tens++)
		{
			for (ones  = '0'; ones <= '9'; ones++
			
